# 자동 동기화 옵션 가이드

## 현재 시스템 (✅ 이미 구현됨)

**크론 기반 자동 동기화**
- 현재: **30분마다** 자동 체크
- 변경: `vercel.json`의 `schedule` 값 수정 가능
- 장점: 설정 간단, 안정적
- 단점: 최대 30분 지연

## 개선 옵션

### 옵션 1: 크론 주기 단축 (권장)

#### 10분마다 체크
```json
"schedule": "*/10 * * * *"
```
- **지연 시간**: 최대 10분
- **API 사용량**: 하루 약 144회 (약 14,400 units)
- **추천**: 일반적인 사용에 적합

#### 5분마다 체크
```json
"schedule": "*/5 * * * *"
```
- **지연 시간**: 최대 5분
- **API 사용량**: 하루 약 288회 (약 28,800 units)
- **추천**: 자주 업로드하는 경우

#### 1분마다 체크
```json
"schedule": "*/1 * * * *"
```
- **지연 시간**: 최대 1분
- **API 사용량**: 하루 약 1,440회 (약 144,000 units)
- **주의**: YouTube API 일일 할당량 (10,000 units) 초과 위험

### 옵션 2: YouTube PubSubHubbub Webhook (실시간)

실시간으로 반영하려면 YouTube PubSubHubbub을 사용할 수 있습니다.

**구현 방법:**
1. YouTube 채널에서 PubSubHubbub 구독 설정
2. 웹훅 엔드포인트 생성 (`/api/webhook/youtube`)
3. 채널에 영상이 업로드되면 즉시 알림 받음
4. 크론 작업 호출하여 동기화

**장점:**
- 실시간 반영 (1분 이내)
- API 호출 최소화

**단점:**
- 구현 복잡도 높음
- 웹훅 검증 로직 필요
- HTTPS 필수 (Vercel에서는 자동 지원)

### 옵션 3: 하이브리드 방식 (추천)

크론 + 수동 트리거 조합:
- 기본: 10분마다 자동 체크
- 필요시: 버튼 클릭으로 즉시 동기화

---

## 현재 설정 변경 방법

`vercel.json` 파일의 `schedule` 값을 수정:

```json
{
  "crons": [
    {
      "path": "/api/cron/sync-shorts",
      "schedule": "*/10 * * * *"  // 10분마다
    }
  ]
}
```

**크론 스케줄 형식 (Cron expression):**
- `*/30 * * * *` - 30분마다
- `*/10 * * * *` - 10분마다
- `*/5 * * * *` - 5분마다
- `*/1 * * * *` - 1분마다

---

## API 할당량 계산

YouTube Data API v3 기본 할당량: **10,000 units/일**

### 크론 작업당 사용량 (대략):
- 채널 정보 조회: ~1 unit
- 재생목록 아이템 조회: ~1 unit
- 영상 상세 조회 (50개): ~50 units
- **총 약 52 units/회**

### 안전한 주기:
- **30분마다**: 하루 약 48회 × 52 = 2,496 units ✅
- **10분마다**: 하루 약 144회 × 52 = 7,488 units ✅
- **5분마다**: 하루 약 288회 × 52 = 14,976 units ⚠️ (할당량 초과 가능)

---

## 추천 설정

### 일반적인 사용 (권장)
```json
"schedule": "*/10 * * * *"
```
- 최대 10분 지연
- API 할당량 안전
- 충분히 빠른 반영

### 자주 업로드하는 경우
```json
"schedule": "*/5 * * * *"
```
- 최대 5분 지연
- API 할당량 주의 필요

---

## 변경 후 배포

1. `vercel.json` 수정
2. Git에 커밋 & 푸시
3. Vercel에서 자동 배포
4. 새로운 크론 스케줄 자동 적용

